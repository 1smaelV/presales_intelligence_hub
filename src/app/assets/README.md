# Static assets

Use this folder for shared static resources (images, icons, and other UI assets). Keep subfolders organized by type (e.g., `images/`, `icons/`, `logos/`) to simplify maintenance and imports.
